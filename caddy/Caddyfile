# LAN-only pseudo-domain for now. Resolve *.media.lan via hosts-file entries or LAN DNS.
# Adding a real domain + `email` directive later gets automatic Let's Encrypt TLS with
# no changes to the site blocks below. Per-site `basicauth`/`forward_auth` can be added
# additively when auth is wanted.

sonarr.media.lan {
	reverse_proxy sonarr:8989
}

radarr.media.lan {
	reverse_proxy radarr:7878
}

prowlarr.media.lan {
	reverse_proxy prowlarr:9696
}

bazarr.media.lan {
	reverse_proxy bazarr:6767
}

jellyfin.media.lan {
	reverse_proxy jellyfin:8096
}

jellyseerr.media.lan {
	reverse_proxy jellyseerr:5055
}

qbittorrent.media.lan {
	reverse_proxy qbittorrent:8080
}
